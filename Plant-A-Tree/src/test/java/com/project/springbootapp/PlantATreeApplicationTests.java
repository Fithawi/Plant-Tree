package com.project.springbootapp;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class PlantATreeApplicationTests {

	@Test
	void contextLoads() {
	}
	/*
	 * @Test public void getUserbyNameTest() { String name = "aser";
	 * when(repository.findByName(name)) .thenReturn(Stream.of(new User(1,
	 * "faser")).collect(Collectors.toList())); assertEquals(1,
	 * service.getUserbyName(name).size()); }
	 * 
	 * 
	 * @Test public void saveUserTest() { User user = new User(999, "Pranya", 33,
	 * "Pune"); when(repository.save(user)).thenReturn(user); assertEquals(user,
	 * service.addUser(user)); }
	 */


}
